name: video_screen
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.11.2
  video_player: ^2.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  photo_manager: ^3.8.3
  geolocator: ^14.0.2
  sqflite: ^2.4.2
  path: ^1.9.1
  local_auth: ^2.1.6

flutter:
  uses-material-design: true

  assets:
    - assets/images/